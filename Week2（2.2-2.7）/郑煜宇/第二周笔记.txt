在 L1-032 Left-pad 中：
	首先，完成相应输入（如果使用cin，读取带有空格的字符串时会出错）
	然后，根据字符串长度和所需长度n的关系，决定对字符串进行切除 / 填充的操作（使用s = s.substr(len-n,n)  /  for (; s.size()<n; ) s = c+s;）

除了必须做的题目之外，还另外写了020-042中比较复杂的题目
L1-028中，素数判断的常用写法可能会忽略输入为1的边界情况
L1-033中使用了<algorithm>:sort，必须参数是指针 / 迭代器（左闭右开），也可以额外输入排序函数进行自定义排序
L1-034中使用了unordered_map，遍历其中的键值对可以使用迭代器写法 for (const auto& pair : m) (不需要include <pair>)
这周重新学习了一下DJ算法，我觉得比弗洛伊德算法更难学...
